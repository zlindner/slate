[phases.setup]
nixPkgs = ['rust-bin.stable', 'openssl']

[phases.install]
cmds = ['cargo prisma generate']

[phases.build]
cmds = ['cargo build --release --package oxy-login']
dependsOn = ['install']

[start]
cmd = 'cargo l'