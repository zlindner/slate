[phases.setup]
nixPkgs = ['gcc', 'rust-bin.stable.latest.default']
aptPkgs = ['openssl', 'openssl-dev', 'libc6']

[phases.install]
cmds = ['cargo prisma generate']

[phases.build]
cmds = ['cargo build --release --package oxy-login']
dependsOn = ['install']

[start]
cmd = 'cargo l'